#pragma once
// 生物类型枚举
enum OrganismType {//大类用于工厂函数
    PLANT,
    ANIMAL,
    ENVIRONMENT,
};

enum EnvironmentType {
    WATER,
    FOREST,
    GRESSLEND,
    MOUTAN,
};

enum OrganismName { //临时占位测试 后面要改成具体的名称
    Organism_Name,
    Reporoducable_Name,
    Plant_Name,
    Animal_Name,
    Environment_Name,
};

enum Weather {
    SUN,
    RAIN,
};